apiVersion: v1
kind: Service
metadata:
  name: veranacouncil-website
  namespace: web
  labels:
    app.kubernetes.io/name: veranacouncil-website
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: veranacouncil-website
